//index.modles

const fetchTopics = require('./topics.models');
const fetchArticles = require('./articles.models');
const fetchUser = require('./user.models');

module.exports = { fetchTopics, fetchArticles, fetchUser };
